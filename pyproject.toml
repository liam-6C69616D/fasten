[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fasten"
version = "0.1.0"

[project.scripts]
fasten = "src.main:app"

[tool.setuptools]
packages = ["src"]
